<UserControl x:Class="MFMFMSF.UI.Controls.SummaryCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Border Background="White"
            BorderBrush="#E8EAF2"
            BorderThickness="1"
            CornerRadius="10"
            Padding="16,14">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <!-- ================================================= -->
            <!-- TOP SECTION -->
            <!-- ================================================= -->

            <Grid Grid.Row="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="42"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>


                <!-- ICON -->

                <Border Grid.Column="0"
                        Width="38"
                        Height="38"
                        CornerRadius="19"
                        Background="{Binding IconBackground,
                            RelativeSource={RelativeSource AncestorType=UserControl}}">

                    <materialDesign:PackIcon
                        Kind="{Binding Icon,
                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Width="21"
                        Height="21"
                        Foreground="{Binding IconForeground,
                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>

                </Border>


                <!-- TITLE + VALUE -->

                <StackPanel Grid.Column="1"
                            Margin="9,0,0,0"
                            VerticalAlignment="Center">

                    <TextBlock
                        Text="{Binding Title,
                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Foreground="#151936"
                        TextTrimming="CharacterEllipsis"/>

                    <TextBlock
                        Text="{Binding Amount,
                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Margin="0,3,0,0"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Foreground="#101432"
                        TextTrimming="CharacterEllipsis"/>

                </StackPanel>

            </Grid>


            <!-- ================================================= -->
            <!-- BOTTOM / TREND -->
            <!-- ================================================= -->

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        Margin="0,12,0,0">


                <!-- TREND ARROW -->

                <TextBlock
                    Text="{Binding TrendArrow,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Foreground="{Binding TrendForeground,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                    VerticalAlignment="Center"/>


                <!-- PERCENTAGE -->

                <TextBlock
                    Text="{Binding Percentage,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Margin="4,0,8,0"
                    FontSize="11"
                    FontWeight="SemiBold"
                    Foreground="{Binding TrendForeground,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                    VerticalAlignment="Center"/>


                <!-- COMPARISON -->

                <TextBlock
                    Text="{Binding Comparison,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="10"
                    Foreground="#68708A"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis"/>

            </StackPanel>

        </Grid>

    </Border>

</UserControl>