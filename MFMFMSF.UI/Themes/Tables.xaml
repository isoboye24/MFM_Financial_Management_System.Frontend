<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================================================== -->
    <!-- TABLE HEADER -->
    <!-- ===================================================== -->

    <Style x:Key="TableHeaderContainer"
           TargetType="Border">

        <Setter Property="Background"
                Value="#F9FAFB"/>

        <Setter Property="BorderBrush"
                Value="{DynamicResource TableBorderBrush}"/>

        <Setter Property="BorderThickness"
                Value="0,0,0,1"/>

    </Style>


    <!-- ===================================================== -->
    <!-- TABLE ROW -->
    <!-- ===================================================== -->

    <Style x:Key="TableRow"
           TargetType="Border">

        <Setter Property="Background"
                Value="{DynamicResource SurfaceBrush}"/>

        <Setter Property="BorderBrush"
                Value="{DynamicResource TableBorderBrush}"/>

        <Setter Property="BorderThickness"
                Value="0,0,0,1"/>

    </Style>


    <!-- ===================================================== -->
    <!-- TABLE ALTERNATE ROW -->
    <!-- ===================================================== -->

    <Style x:Key="AlternateTableRow"
           TargetType="Border">

        <Setter Property="Background"
                Value="{DynamicResource TableAlternateRowBrush}"/>

        <Setter Property="BorderBrush"
                Value="{DynamicResource TableBorderBrush}"/>

        <Setter Property="BorderThickness"
                Value="0,0,0,1"/>

    </Style>


    <!-- ===================================================== -->
    <!-- TABLE ACTION BUTTON -->
    <!-- ===================================================== -->

    <Style x:Key="TableActionButton"
           TargetType="Button">

        <Setter Property="Width"
                Value="30"/>

        <Setter Property="Height"
                Value="30"/>

        <Setter Property="Padding"
                Value="0"/>

        <Setter Property="Background"
                Value="Transparent"/>

        <Setter Property="BorderThickness"
                Value="0"/>

        <Setter Property="Foreground"
                Value="{DynamicResource SecondaryTextBrush}"/>

    </Style>


    <!-- ===================================================== -->
    <!-- TABLE BADGE -->
    <!-- ===================================================== -->

    <Style x:Key="TableBadge"
           TargetType="Border">

        <Setter Property="CornerRadius"
                Value="7"/>

        <Setter Property="Padding"
                Value="8,3"/>

        <Setter Property="HorizontalAlignment"
                Value="Left"/>

        <Setter Property="VerticalAlignment"
                Value="Center"/>

    </Style>


    <!-- ===================================================== -->
    <!-- TABLE PAGINATION -->
    <!-- ===================================================== -->

    <Style x:Key="PaginationButton"
           TargetType="Button">

        <Setter Property="Width"
                Value="32"/>

        <Setter Property="Height"
                Value="32"/>

        <Setter Property="Margin"
                Value="3,0"/>

        <Setter Property="Padding"
                Value="0"/>

        <Setter Property="Background"
                Value="Transparent"/>

        <Setter Property="Foreground"
                Value="{DynamicResource SecondaryTextBrush}"/>

        <Setter Property="BorderThickness"
                Value="0"/>

        <Setter Property="FontFamily"
                Value="Segoe UI"/>

        <Setter Property="FontSize"
                Value="12"/>

    </Style>


    <!-- ===================================================== -->
    <!-- CURRENT PAGINATION BUTTON -->
    <!-- ===================================================== -->

    <Style x:Key="PaginationCurrentButton"
           TargetType="Button">

        <Setter Property="Width"
                Value="32"/>

        <Setter Property="Height"
                Value="32"/>

        <Setter Property="Margin"
                Value="3,0"/>

        <Setter Property="Padding"
                Value="0"/>

        <Setter Property="Background"
                Value="{DynamicResource PrimaryBrush}"/>

        <Setter Property="Foreground"
                Value="White"/>

        <Setter Property="BorderThickness"
                Value="0"/>

        <Setter Property="FontFamily"
                Value="Segoe UI"/>

        <Setter Property="FontSize"
                Value="12"/>

    </Style>


    <!-- ===================================================== -->
    <!-- STRIPED TABLE ROW -->
    <!-- ===================================================== -->

    <Style x:Key="TableStripedRow"
       TargetType="Border">

        <!-- NORMAL ROW -->

        <Setter Property="Background"
            Value="{DynamicResource SurfaceBrush}"/>

        <Setter Property="BorderBrush"
            Value="{DynamicResource TableBorderBrush}"/>

        <Setter Property="BorderThickness"
            Value="0,0,0,1"/>

        <Setter Property="Padding"
            Value="0,10"/>


        <Style.Triggers>

            <!-- =============================================== -->
            <!-- ALTERNATE ROW -->
            <!-- =============================================== -->

            <DataTrigger
            Binding="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter},
                              Path=(ItemsControl.AlternationIndex)}"
            Value="1">

                <Setter
                Property="Background"
                Value="{DynamicResource TableAlternateRowBrush}"/>

            </DataTrigger>


            <!-- =============================================== -->
            <!-- ROW HOVER -->
            <!-- =============================================== -->

            <Trigger
            Property="IsMouseOver"
            Value="True">

                <Setter
                Property="Background"
                Value="{DynamicResource PrimaryHoverBackgroundBrush}"/>

                <Setter
                Property="BorderBrush"
                Value="{DynamicResource PrimaryBrush}"/>

            </Trigger>

        </Style.Triggers>

    </Style>


    <!-- ===================================================== -->
    <!-- TABLE DELETE ACTION BUTTON -->
    <!-- ===================================================== -->

    <Style x:Key="TableDeleteActionButton"
       TargetType="Button">

        <Setter Property="Width"
            Value="30"/>

        <Setter Property="Height"
            Value="30"/>

        <Setter Property="MinWidth"
            Value="30"/>

        <Setter Property="Padding"
            Value="0"/>

        <Setter Property="Margin"
            Value="3,0"/>

        <Setter Property="Background"
            Value="Transparent"/>

        <Setter Property="Foreground"
            Value="{DynamicResource DangerBrush}"/>

        <Setter Property="BorderThickness"
            Value="0"/>

        <Setter Property="Cursor"
            Value="Hand"/>

        <Setter Property="Template">

            <Setter.Value>

                <ControlTemplate TargetType="Button">

                    <Border
                    x:Name="ActionBorder"
                    Background="{TemplateBinding Background}"
                    CornerRadius="6">

                        <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>

                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- HOVER -->

                        <Trigger
                        Property="IsMouseOver"
                        Value="True">

                            <Setter
                            TargetName="ActionBorder"
                            Property="Background"
                            Value="#FFF1F2"/>

                        </Trigger>


                        <!-- PRESSED -->

                        <Trigger
                        Property="IsPressed"
                        Value="True">

                            <Setter
                            TargetName="ActionBorder"
                            Property="Background"
                            Value="#FFE4E6"/>

                        </Trigger>


                        <!-- DISABLED -->

                        <Trigger
                        Property="IsEnabled"
                        Value="False">

                            <Setter
                            TargetName="ActionBorder"
                            Property="Opacity"
                            Value="0.5"/>

                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>

        </Setter>

    </Style>

</ResourceDictionary>