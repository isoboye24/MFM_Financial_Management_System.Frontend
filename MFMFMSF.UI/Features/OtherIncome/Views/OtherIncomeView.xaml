<UserControl x:Class="MFMFMSF.UI.Features.OtherIncome.Views.OtherIncomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MFMFMSF.UI.Features.OtherIncome.Views" 
             xmlns:lvc1="clr-namespace:MFMFMSF.UI.Controls" 
             xmlns:lvc2="clr-namespace:MFMFMSF.UI.Features.OtherIncome.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Background="#F5F7FB">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
            Height="Auto"
            Background="White"
            CornerRadius="15"
            Margin="0,0,0,20">

            <lvc1:PageTopBar
                PageTitle="Other Income"
                PageDescription="View and manage all other income received by the church."
                DateRangeText="May 1 – May 31, 2025"
                ActionText="Add Other Income"
                ActionCommand="{Binding AddTitleCommand}"
                DateRangeCommand="{Binding SelectDateRangeCommand}"/>

        </Border>

        <Border Grid.Row="1"
            Height="Auto"
            CornerRadius="15"
            Padding="2"
            Margin="0,0,0,20">

            <lvc2:OtherIncomeStatCards/>

        </Border>

        <Border Grid.Row="2">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                Height="Auto"
                CornerRadius="15"
                Padding="2">

                    <lvc2:OtherIncomeTable/>

                </Border>

                <Border Grid.Column="1" Padding="20,0,0,0">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="250"/>
                            <RowDefinition Height="Auto"/>                            
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                        Height="Auto"
                        CornerRadius="15"
                        Padding="2"
                        Margin="0,0,0,20">

                            <lvc2:OtherIncomeSideCards/>

                        </Border>


                        <Border Grid.Row="1"
                        Height="Auto"
                        CornerRadius="15"
                         Margin="0,0,0,20">

                            <lvc2:OtherIncomeQuickActionCards/>

                        </Border>
                        
                        <Border Grid.Row="2"
                        Height="Auto"
                        CornerRadius="15"
                        Margin="0,0,0,20">

                            <lvc2:OtherIncomeProgressBars/>

                        </Border>
                        
                       

                    </Grid>
                </Border>

                
            </Grid>
        </Border>

        <Border Grid.Row="3"
            Height="300"
            CornerRadius="15"
            Margin="0,20,0,20"> 

            <lvc2:OtherIncomeTrend/>

        </Border>

    </Grid>
</UserControl>
