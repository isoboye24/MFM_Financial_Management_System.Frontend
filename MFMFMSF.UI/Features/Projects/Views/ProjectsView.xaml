<UserControl x:Class="MFMFMSF.UI.Features.Projects.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MFMFMSF.UI.Features.Projects.Views" xmlns:lvc1="clr-namespace:MFMFMSF.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
              Background="#F5F7FB">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
            Height="Auto"
            Background="White"
            CornerRadius="15"
            Margin="0,0,0,20">

            <lvc1:PageTopBar
                PageTitle="Projects"
                PageDescription="Track and manage all church projects and initiatives."
                DateRangeText="May 1 – May 31, 2025"
                ActionText="Add Project"
                ActionCommand="{Binding AddTitleCommand}"
                DateRangeCommand="{Binding SelectDateRangeCommand}"/>

        </Border>

        <Border Grid.Row="1"
            Height="Auto"
            CornerRadius="15"
            Margin="0,0,0,20">

            <lvc1:StatisticCards/>

        </Border>
        
        
        
    </Grid>
</UserControl>
