<UserControl x:Class="MFMFMSF.UI.Features.Givings.Views.ViewSingleGiving"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:controls="clr-namespace:MFMFMSF.UI.Controls" 
             xmlns:controls1="clr-namespace:MFMFMSF.UI.Features.Givings.Controls"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1100"
             Background="#F5F7FB">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- ===================================================== -->
        <!-- PAGE HEADER -->
        <!-- ===================================================== -->

        <Border Grid.Row="0"
            Margin="0,0,0,20"
                Padding="20"
                Background="White">

            <controls:FormTitle
                BreadcrumbText="Church Giving"
                CurrentPageText="View Giving"
                Title="View Giving"
                Description="View the complete information about this giving."
                NavigationService="{Binding NavigationService,
                                           RelativeSource={RelativeSource AncestorType=UserControl}}"/>

        </Border>


        <!-- ===================================================== -->
        <!-- ATTENDANCE CARDS -->
        <!-- ===================================================== -->

        <Border Grid.Row="1"
            Margin="0,0,0,20">
            <controls1:ViewGivingAttendanceCards/>
        </Border>


        <!-- ===================================================== -->
        <!-- MAIN INFORMATION -->
        <!-- ===================================================== -->

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <!-- ================================================= -->
            <!-- GIVING INFORMATION -->
            <!-- ================================================= -->

            <Border Grid.Column="0"
                    Background="White"
                    BorderBrush="#E8EAF2"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="20">

                <StackPanel>

                    <TextBlock
                        Text="Giving Information"
                        FontSize="20"
                        FontWeight="SemiBold"
                        Foreground="#151936"/>

                    <TextBlock
                        Text="Details about this church giving."
                        FontSize="16"
                        Foreground="#68708A"
                        Margin="0,4,0,35"/>


                    <!-- CATEGORY -->

                    <TextBlock
                        Text="Giving Category"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#68708A"/>

                    <TextBlock
                        Text="{Binding Giving.CategoryName}"
                        FontSize="16"
                        Foreground="#303653"
                        Margin="0,5,0,30"/>


                    <!-- AMOUNT -->

                    <TextBlock
                        Text="Amount"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#68708A"/>

                    <TextBlock
                        Text="{Binding Giving.Amount, StringFormat={}{0:N2}}"
                        FontSize="20"
                        FontWeight="SemiBold"
                        Foreground="#5B4BEB"
                        Margin="0,5,0,30"/>


                    <!-- DATE -->

                    <TextBlock
                        Text="Date"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#68708A"/>

                    <TextBlock
                        Text="{Binding Giving.Date, StringFormat={}{0:dd/MM/yyyy}}"
                        FontSize="16"
                        Foreground="#303653"
                        Margin="0,5,0,0"/>

                </StackPanel>

            </Border>


            <!-- ================================================= -->
            <!-- SERVICE INFORMATION -->
            <!-- ================================================= -->

            <Border Grid.Column="2"
                    Background="White"
                    BorderBrush="#E8EAF2"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="20">

                <StackPanel>

                    <TextBlock
                        Text="Service Information"
                        FontSize="20"
                        FontWeight="SemiBold"
                        Foreground="#151936"/>

                    <TextBlock
                        Text="Information about the church service."
                        FontSize="16"
                        Foreground="#68708A"
                        Margin="0,4,0,35"/>


                    <!-- MESSAGE -->

                    <TextBlock
                        Text="Message Title"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#68708A"/>

                    <TextBlock
                        Text="{Binding Giving.MessageTitle}"
                        FontSize="16"
                        Foreground="#303653"
                        TextWrapping="Wrap"
                        Margin="0,5,0,30"/>


                    <!-- MINISTER -->

                    <TextBlock
                        Text="Minister"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#68708A"/>

                    <TextBlock
                        Text="{Binding Giving.Minister}"
                        FontSize="16"
                        Foreground="#303653"
                        Margin="0,5,0,0"/>

                </StackPanel>

            </Border>

        </Grid>


        <!-- ===================================================== -->
        <!-- SUMMARY -->
        <!-- ===================================================== -->

        <Border Grid.Row="3"
                Background="White"
                BorderBrush="#E8EAF2"
                BorderThickness="1"
                CornerRadius="12"
                Padding="20"
                Margin="0,20,0,0">

            <StackPanel>

                <TextBlock
                    Text="Summary"
                    FontSize="14"
                    FontWeight="Bold"
                    Foreground="#151936"/>

                <TextBlock
                    Text="{Binding Giving.Summary}"
                    FontSize="16"
                    Foreground="#303653"
                    TextWrapping="Wrap"
                    Margin="0,8,0,0"/>

            </StackPanel>

        </Border>

    </Grid>

</UserControl>