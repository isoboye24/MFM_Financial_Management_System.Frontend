<UserControl x:Class="MFMFMSF.UI.Features.Dashboard.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:MFMFMSF.UI.Controls" 
             xmlns:lvc="clr-namespace:MFMFMSF.UI.Features.Dashboard.Controls" 
             xmlns:viewmodels="clr-namespace:MFMFMSF.UI.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1400">

    <Grid Background="#F5F7FB">

        <!-- Page Margin -->
        <Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="300"/>
                <RowDefinition Height="350"/>
            </Grid.RowDefinitions>

            <!-- ================= TOP BAR ================= -->

            <Border Grid.Row="0"
                    Height="60"
                    Background="White"
                    CornerRadius="15"
                    Padding="2"
                    Margin="0,0,0,20">

                <lvc:TopWelcomeBar/>

            </Border>

            <!-- ================= STATISTICS ================= -->

            <Border Grid.Row="1"
            Padding="2"
            Margin="0,0,0,20">

                <controls:StatisticCards/>

            </Border>

            <!-- ================= CHARTS ================= -->

            <Grid Grid.Row="2"
                  Margin="0,0,0,20">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="White"
                        CornerRadius="18"
                        Padding="10">
                    <lvc:IncomeChart/>
                </Border>

                <Border Grid.Column="2"
                        Background="White"
                        CornerRadius="18"
                        Padding="20">

                    <lvc:IncomeVsExpenditure/>

                </Border>

            </Grid>

            <!-- ================= BOTTOM ================= -->

            <Grid Grid.Row="3">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="White"
                        CornerRadius="18"
                        Padding="20">

                    <lvc:RecentRecords/>

                </Border>

                <Border Grid.Column="2"
            Background="White"
            CornerRadius="18"
            Padding="20">

                    <lvc:QuickActionCards/>

                </Border>

            </Grid>

        </Grid>

    </Grid>

</UserControl>