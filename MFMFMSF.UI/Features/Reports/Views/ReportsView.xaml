<UserControl x:Class="MFMFMSF.UI.Features.Reports.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MFMFMSF.UI.Features.Reports.Views"
             xmlns:lvc1="clr-namespace:MFMFMSF.UI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="#F5F7FB">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- ================= TOP BAR ================= -->

        <Border Grid.Row="0"
                Background="White"
                CornerRadius="15"
                Margin="0,0,0,20">

            <lvc1:PageTopBar
                PageTitle="Reports"
                PageDescription="View and analyse church financial reports."
                DateRangeText="May 1 – May 31, 2025"
                ActionText="Add Custom Reports"
                ActionCommand="{Binding AddTitleCommand}"
                DateRangeCommand="{Binding SelectDateRangeCommand}"/>

        </Border>


        <!-- ================= STATISTICS ================= -->

        <Border Grid.Row="1"
                Background="Transparent"
                CornerRadius="15"
                Margin="0,0,0,20">

            <lvc1:StatisticCards/>

        </Border>


        <!-- ================= REPORT SECTION ================= -->

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10">

            <Grid>

                <Grid.RowDefinitions>
                    <!-- Tabs -->
                    <RowDefinition Height="50"/>

                    <!-- Report content -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <!-- ================= TABS ================= -->

                <lvc1:PageTabs
                    Grid.Row="0"
                    SelectedIndex="0"
                    TabChanged="PageTabs_TabChanged"/>


                <!-- ================= REPORT CONTENT ================= -->

                <ContentControl
                    x:Name="ReportContent"
                    Grid.Row="1"
                    Margin="0,15,0,0"/>

            </Grid>

        </Border>

    </Grid>

</UserControl>